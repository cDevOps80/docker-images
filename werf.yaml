project: demo
configVersion: 1

image: dfrontend
dockerfile: frontend-dockerfile
context: roboshop-v1
git:
  - url: https://github.com/your-repo/roboshop-v1.git  # Add your repository URL
    branch: main
    add: /
    to: /app

shell:
  beforeInstall:
    - mkdir -p /app
    - chmod -R 755 /app
 # Add actual test/setup commands

